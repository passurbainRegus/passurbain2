swagger: "2.0"
info:
  version: 1.0.0
  title: PassUrbain
  description: Historique Usager
  termsOfService: http://swagger.io/terms/
  contact:
    name: Swagger API Team
    email: foo@example.com
    url: http://madskristensen.net
  license:
    name: MIT
    url: http://github.com/gruntjs/grunt/blob/master/LICENSE-MIT
host: petstore.swagger.io
basePath: /api
schemes:
  - http
consumes:
  - application/json
produces:
  - application/json
paths:
  /historique_usager:
    get:
      description: |
        Returns all historique_usager's from the system that the user has access to 
      operationId: findHistorique_Usager
      parameters:
        - name: tags
          in: query
          description: tags to filter by
          required: false
          type: array
          collectionFormat: csv
          items:
            type: string
        - name: limit
          in: query
          description: maximum number of results to return
          required: false
          type: integer
          format: int32
      responses:
        200:
          description: historique_usager response
          schema:
            type: array
            items:
              $ref: '#/definitions/HistoriqueUsager'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
    post:
      description: Creates a new historique_usager.
      operationId: addHistoriqueUsager
      parameters:
        - name: historique_usager
          in: body
          description: HistoriqueUsager to add to the PU
          required: true
          schema:
            $ref: '#/definitions/NewHistoriqueUsager'
      responses:
        200:
          description: historique_usager response
          schema:
            $ref: '#/definitions/HistoriqueUsager'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
  /historique_usager/{id_hu}:
    get:
      description: Returns a historique_usager based on a single ID
      operationId: find historique_usager by id_hu
      parameters:
        - name: id_hu
          in: path
          description: ID of historique_usager to fetch
          required: true
          type: integer
          format: int64
      responses:
        200:
          description: historique_usager response
          schema:
            $ref: '#/definitions/HistoriqueUsager'
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
    delete:
      description: deletes a single historique_usager based on the ID supplied
      operationId: deleteHistoriqueUsager by id_hu
      parameters:
        - name: id_hu
          in: path
          description: ID of historique_usager to delete
          required: true
          type: integer
          format: int64
      responses:
        204:
          description: historique_usager deleted
        default:
          description: unexpected error
          schema:
            $ref: '#/definitions/Error'
definitions:
  HistoriqueUsager:
    allOf:
      - $ref: '#/definitions/NewHistoriqueUsager'
      - required:
        - id_hu
        properties:
          id_hu:
            type: integer
            format: int64
  NewHistoriqueUsager:
    properties:
      id_usager_hu:
        type: integer
        format: int64
      id_CUT_usager_hu:
        type: integer
        format: int64
      no_usager_hu:
        type: string
      prenom_usager_hu:
        type: string
      adresse_email_usager_hu:
        type: string
      sece_usager_hu:
        type: string
      date_naissance_usager_hu:
        type: string
      lieu_naissance_usager_hu_:
        type: string
      pays_naissance_usager_hu:
        type: string
      nom_usage_usager_hu:
        type: string
      adresse_postale_usager_hu:
        type: string
      numero_telephone_usager_hu:
        type: string
  Error:
    required:
      - code
      - message
    properties:
      code:
        type: integer
        format: int32
      message:
        type: string